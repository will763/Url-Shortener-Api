export const config = {
    API_URL: "https://encurta",
    MONGO_CONNECTION : `mongodb+srv://${process.env.USER}:${process.env.PASSWORD}@cluster0.q24mx.mongodb.net/cluster0?retryWrites=true&w=majority`
}